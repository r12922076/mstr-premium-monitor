name: Update market data

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  smoke-test:
    runs-on: ubuntu-latest
    steps:
      - name: Smoke test
        run: |
          echo "workflow_dispatch works"
